using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnchantmentCard : PlayedCard
{
    private ArrayList allowCardTypes;

    public ArrayList AllowCardTypes { get => allowCardTypes; set => allowCardTypes = value; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
